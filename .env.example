# Backend SurrealDB Configuration
# Set these to enable the built-in backend SurrealDB instance
BACKEND_SURREALDB_URL=ws://surrealdb:8000
BACKEND_SURREALDB_PORT=8001

# Default namespace and database when using backend
DEFAULT_NAMESPACE=summer
DEFAULT_DATABASE=summer

# Server configuration
PORT=3000
HOST=0.0.0.0
